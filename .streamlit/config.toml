[theme]
# 暗色主題
base = "dark"
font = "sans serif"

# 整個 App 的根字體大小（單位 px，預設 16）。
# 這是目前唯一真的能改變 st.dataframe 表格文字大小的方式，
# 但它是「整站通用」的設定：標題、按鈕、側邊欄文字也會一起縮放，
# 沒辦法只單獨調整表格。改這個數字、重新部署後就會生效，
# 不需要動到程式碼。
baseFontSize = 12
