[theme]
# 調整全站基礎字體大小為小號，Streamlit 的 DataFrame 會自動跟著縮小字體！
base = "dark" # 如果你是暗色主題，可用 dark；亮色用 light
font = "sans serif"

# 強制將 Streamlit 的 UI 與表格字型縮小
[ui]
# 這裡能把全站元件的預設緊湊度拉到最高，讓畫面能容納更多資訊
density = "compact"
